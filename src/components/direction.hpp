#ifndef IMPINHEAVEN_DIRECTION_HPP
#define IMPINHEAVEN_DIRECTION_HPP

struct Direction {
    Direction(float x = 0.0f, float y = 0.0f): x(x), y(y) {}

    float x, y;
};

#endif //IMPINHEAVEN_DIRECTION_HPP
